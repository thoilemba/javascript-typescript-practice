let name = "Thoilemba"; // String
let age = 25; // number
let weight = 61.5; // number, there is no float or double explicitly
let firstName = undefined; // undefined
let lastName = null; // null is an object

console.log(name, age, weight, firstName, lastName);
console.log(typeof name, typeof age, typeof weight, typeof firstName, typeof lastName);